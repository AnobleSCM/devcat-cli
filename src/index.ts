/**
 * DevCat CLI entry point.
 *
 * Bin file lives at src/bin/devcat.ts (added in Plan 39-02 — sync command + arg parsing).
 * This module is the package's main field target for programmatic use; the CLI itself runs via the bin.
 */

export const PACKAGE_NAME = 'devcat';
